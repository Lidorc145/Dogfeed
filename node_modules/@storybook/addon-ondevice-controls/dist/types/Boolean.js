import { View, Switch, StyleSheet } from 'react-native';
import React from 'react';
const BooleanType = ({ arg, onChange }) => (React.createElement(View, { style: styles.switch },
    React.createElement(Switch, { testID: arg.name, onValueChange: () => onChange(!arg.value), value: arg.value })));
const styles = StyleSheet.create({
    switch: { margin: 10, alignItems: 'flex-start' },
});
BooleanType.serialize = (value) => (value ? String(value) : null);
BooleanType.deserialize = (value) => value === 'true';
export default BooleanType;
