import { View } from 'react-native';
import React from 'react';
import RadioSelect from '../components/RadioSelect';
const RadioType = ({ onChange, arg, isInline }) => {
    const getOptions = ({ options }) => {
        if (Array.isArray(options)) {
            return options.map((val) => ({ key: val, label: val }));
        }
        return Object.keys(options).map((key) => ({
            label: key,
            key: options[key],
        }));
    };
    const options = getOptions(arg);
    return (React.createElement(View, null,
        React.createElement(RadioSelect, { isInline: isInline, data: options, value: arg.value, onChange: (option) => onChange(option.key) })));
};
RadioType.serialize = (value) => value;
RadioType.deserialize = (value) => value;
export default RadioType;
